# Copy this file to .env and fill in real keys. .env is gitignored — never commit real keys,
# and never hardcode them in source (instant fail per subject).

OPENROUTER_API_KEY=
GROQ_API_KEY=
TOGETHER_API_KEY=
GOOGLE_AI_STUDIO_API_KEY=
